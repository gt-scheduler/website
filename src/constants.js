const OPEN = 8 * 60;
const CLOSE = 21 * 60;

const TYPE_LECTURE = 'Lecture';
const TYPE_LAB = 'Supervised Laboratory';

const PNG_SCALE_FACTOR = 2;

export {
  OPEN,
  CLOSE,
  TYPE_LECTURE,
  TYPE_LAB,
  PNG_SCALE_FACTOR,
};
